# EpistemicOps — HuggingFace Spaces Dependencies
# Minimal set needed for the Gradio demo dashboard
# Gradio 5.9.x crashes on /info when JSON Schema uses boolean additionalProperties (Pydantic / Py3.13).
gradio==5.50.0
pandas>=2.0.0
matplotlib>=3.8.0
numpy>=1.24.0
pyyaml>=6.0.1
# Live Simulation tab (run_episode → agents + drift injector)
openai>=1.40.0
httpx>=0.27.0
